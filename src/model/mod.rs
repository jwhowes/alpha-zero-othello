mod vit;

use crate::board::Board;

/*
 Evaluates a board position, returning the prior probabilities and non-subjective value 
*/
pub async fn evaluate_board(board: &Board) -> (Vec<f32>, f32) {
    todo!()
}